version: '3'
services:
  app:
    container_name: ${NAME}
    image: python
    #restart: always
    volumes:
      - type: bind
        source: ./code
        target: /app
    build: 
      context: .
      dockerfile: Dockerfile
    environment:
    - "TEST=${TEST}"